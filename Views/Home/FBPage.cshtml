@model FizzBuzzMVC.Models.FizzBuzz


<main class="flex-shrink-0">
    <div class="container px-4 py-5">
        <div class="row g-5 py-5 row-cols-1 row-cols-lg-2">
            <div class="col">
                <h1 class="display-5 fw-bold lh-1 mb-3">Fizz Buzz MVC: A Coding Project</h1>
                <h4 class="lead">This application will consider the two numbers entered and for each number between 1 and 100. It will print <strong>FIZZ</strong> for the numbers that are divisibel by by the first number. <strong>BUZZ</strong> will be printed for number divisible by the secon number. <strong>FIZZBUZZ</strong> will be printed for the numbers divisible by both number and simply print the number if is not divisible by either.</h4>
                <h2 class="mt-5 border-bottom border-2">Instruction</h2>
                <p>Enter two numbers. The first number will be used to evaluate <strong>FIZZ</strong>. The second number will be used to evaluate <strong>BUZZ</strong>. <strong>FIZZBUZZ</strong> will be determined based on the combination of the two numbers.</p>
            </div>    
            <div class="col">
                <div>
                    <h5>Enter Numbers</h5>
                    @* *** Form Goes Here *** *@
                    <form asp-controller="Home" asp-action="FBPage" method="post">
                        <div class="form-group">
                            <label asp-for="FizzValue">Fizz Value</label>
                            <input type="number" asp-for="FizzValue" class="form-control" placeholder="Enter Fizz" value="@Model.FizzValue" />
                        </div>
                        <div class="form-group">
                            <label asp-for="BuzzValue">Fizz Value</label>
                            <input type="number" asp-for="BuzzValue" class="form-control" placeholder="Enter Buzz" value="@Model.BuzzValue" />
                        </div>
                        <button type="submit" class="btn btn-dark">Submit</button>
                    </form>
                    <h2 class="mt-5 border-bottom border-2">Results</h2>
                    <div class="table-responsive">
                    @* *** Table Goes Here *** *@

                    </div>
                </div>
              
               
               
            </div>
        </div>
    </div>
</main>